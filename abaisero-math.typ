// math

#let naturalset = $bb(N)$
#let integerset = $bb(Z)$
#let realset = $bb(R)$

#let comp(content) = $#content^c$

#let kstar(content) = $#content^*$
#let kplus(content) = $#content^+$

#let setminus = $backslash$

#let softmax = $op("softmax")$
#let softmin = $op("softmin")$
#let sign = $op("sign")$

#let evaluated(content) = $lr(content|, size: #150%)$
